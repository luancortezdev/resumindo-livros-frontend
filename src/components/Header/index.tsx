import { Typography } from '@mui/material'
import { Box } from '@mui/system'

export default function Header() {
  return (
    <Box paddingTop={'20px'}>
      <Typography variant='h1' fontSize='40px' textAlign='initial' color={'#29EBAA'}>
        RESUMINDO CAPÍTULOS
      </Typography>
    </Box >
  )
}
